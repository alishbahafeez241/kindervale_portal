import { ExactLogin } from "@/components/auth/exact-login";

export const metadata = {
  title: "Login — Kindervale Preschool",
  description: "Sign in to access Kindervale Preschool portal."
};

export default function LoginPage() {
  return <ExactLogin />;
}
